
notepad-app
===========

* NoteDetailActivity のリファクタリング
  * NoteDetailViewer
  * NoteDetailEditor

* フォントサイズのカスタマイズ
  * 必要か？
  * ノート一覧画面
  * ノート表示画面
  * ノート編集画面

* ソート順の設定が反映していないバグを修正
  * PickNoteActivity のソート順を修正
  * DeleteNotesActivity のソート順を修正

* ノート一覧画面でノート表示順を変更可能にする
  * 現在のソート順をどのように表示するか？
    * ノート一覧画面にヘッダをつけてそこに表示する？

#* 汎用のインテントを受信してノート作成
#  * text/* の SEND を受信するようにする？
#  * 受信処理専用アクティビティを作成
#    * ReceiveTextActivity
#    * 受信後にノート追加用のインテントを投げる？
#      * Action : Intent.ACTION_INSERT
#      * Type : "vnd.android.cursor.dir/vnd.routine_work.note"
#      * Extra
#        * EXTRA_TITLE
#        * EXTRA_TEXT
#  * 設定画面で有効無効切り替えられるように
#    * ReceiveTextActivity の有効無効を行う？
#    * 設定値を用意
#    * 変更時のリスナーでコンポーネントの有効無効を変更

#* バックアップファイルのファイル名の時刻部分のフォーマットを修正
#  * 24時間じゃなくて12時間表記になっているような…


#* データ同期機能実装のためのデータベース構造の変更
#  * すべてのデバイスでユニークなノートIDが欲しい
#  * UUIDをつける？
#    * ノート作成時にノート毎にUUIDを生成する
#  * 削除をフラグにする
#    * enabled カラムを追加
#  * カラムを追加(uuid, state)
#    * notepad-common
#      * NoteStoreを修正
#        * NoteStore.Note.Columns に UUID を追加
#      * NoteDBConstants を修正
#        * DATABASE_VERSION
#        * Notes.CREATE_TABLE_SQL
#      * Note を修正
#        * インスタンス変数 uuid を追加
#        * メソッド hashCode() を修正
#        * メソッド equals() を修正
#        * メソッド toString() を修正
#        * メソッド copyFrom() を修正
#      * NoteDBHelper を修正
#        * backupNotesVersion4() 修正
#        * backupNotesVersion5() 修正
#        * backupNotesVersion12() 追加
#        * restoreNotes() 修正
#      * NoteProvider を修正
#        * insertNote() を UUID がなければ生成するように修正

#* 削除処理を変更
#  * delete していたものを enabled = false にする処理に修正
#  * notepad-common
#    * NoteStore#deleteNotes(ContentResolver cr, Collection<Longo> idCollection) を修正
#    * NoteStore#deleteNotes(ContentResolver cr, long[] ids) を修正
#    * NoteStore#deleteNote(ContentResolver cr, Uri uri) を作成
#  * notepad-app
#    * NoteDetailActivity#deleteNote() を修正
#  * notepad-app-level-11
#    * DeleteNoteFragment#deleteNote() を修正

* データ参照処理を修正
  * enabled = true のものだけを表示するように修正
  * notepad-common
    * NoteStore#queryEnabledNotes(ContentResolver cr, Uri uri) を作成
  * notepad-app
    * PickNoteActivity#initializeListData() を修正
    * DeleteNotesActivity#initializeListData() を修正
    * NotepadActivity#updateContentWithUri() を修正
  * notepad-app-level-11
    * PickNoteActivity#onCreateLoader() を修正
    * DeleteNotesFragments#onCreateLoader() を修正
    * NoteListFragments#onCreateLoader() を修正


notepad-app-level-11
====================

* アクションバー自動非表示時にジェスチャーで表示できるようにする
  * ScrollView の上端にいる場合に下にスワイプしたら表示？

* NotepadPreferenceActivity をフラグメントにする？



notepad-common
==============

* TimeFormatUtils 修正
  * 西暦部分のないフォーマットにできないか？

* NoteSearchQueryParser に日付記法とかもあってもいいかも
  * 書式
    * mt:<yyyymmdd[HH[MM[SS]]]>
    * ct:<yyyymmdd[HH[MM[SS]]]>
  * 手順
    * ContentProvider 側で処理できるようなURIを決める
      * contents://org.routine_work.notepad.noteprovider/notes?mt=<yyyymmddHHMMSS>-<yyyymmddHHMMSS>
      * contents://org.routine_work.notepad.noteprovider/notes?ct=<yyyymmddHHMMSS>-<yyyymmddHHMMSS>
    * ContentProvider 側の処理を実装
    * NoteSearchQueryParser にパース機能を実装
    * NoteSearchQueryParser を利用するコードの修正


Documents
=========

* インテント呼び出しのドキュメントを作成
  * 受信するインテントについて
    * ノート一覧を検索
    * ノート一覧に追加
    * 指定ノートの表示
    * 指定ノートの編集
    * 指定ノートの削除
    * テキスト送信
  * 送信するインテントについて
* コンテントプロバイダの使い方のドキュメントを作成

